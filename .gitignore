settings.json
.venv/
__pycache__/
*.pyc
.DS_Store
save/
runtime_memory/
test-saves/
# --log-llm transcripts: full prompts and replies, local only
logs/

# founding design doc — kept locally, not shipped
ORIGINAL_GAME_SETTING.md

# Player preferences (local, not part of the project)
prefs.json
